# AWS Layer Externals Terraform module

Terraform module which creates the resources to grant externals to access this aws account

## Usage

```hcl
module "main" {
  source      = "genstackio/layer-externals/aws"

  // ...
}
```
